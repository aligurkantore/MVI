package com.example.mvi.common.extensions

/*
fun Fragment.handleFailViewType(
    message: Int,
    failViewType: FailViewType,
) {
    val errorMessage = try {
        getString(message)
    } catch (e: Resources.NotFoundException) {
        getString(0)
    }

    when (failViewType) {
        FailViewType.Popup -> {
            showPopup(title = getString(0), desc = errorMessage)
        }

        FailViewType.FullPopup -> {
            showFullScreenPopUp(
                getString(0),
                errorMessage
            )
        }

        FailViewType.None -> {}
    }
}

 */